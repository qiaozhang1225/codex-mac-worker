interface:
  display_name: "派发 Codex Worker 任务"
  short_description: "Mac mini 派单、onboarding、review 与受控合并"
  default_prompt: "Use $dispatch-codex-task to onboard, dispatch, review, or explicitly merge one bounded Mac mini Worker task."
